function addStudents(params) {
    let parentslist = document.querySelector("li");

    let newParents = document.createElement("li");
    newParents.textContent = name;
    parentslist.appendstudentd(newParents);
}
addStudents('Ayxan Eliyev');